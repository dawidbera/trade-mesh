# Analytics Service Configuration
quarkus.http.port=8082
quarkus.grpc.server.port=9002

# gRPC Client for Market Data Service
quarkus.grpc.clients.market.host=localhost
quarkus.grpc.clients.market.port=9001

# Redis Configuration (Quarkus DevServices will start Redis in dev/test if hosts are not defined)
%prod.quarkus.redis.hosts=redis://redis:6379

# RabbitMQ Configuration
mp.messaging.incoming.market-prices.connector=smallrye-rabbitmq
mp.messaging.incoming.market-prices.exchange.name=market-prices
mp.messaging.incoming.market-prices.exchange.type=fanout
mp.messaging.incoming.market-prices.queue.name=analytics-price-queue

# Production host for RabbitMQ
%prod.mp.messaging.connector.smallrye-rabbitmq.host=rabbitmq
%prod.mp.messaging.connector.smallrye-rabbitmq.username=trademesh
%prod.mp.messaging.connector.smallrye-rabbitmq.password=trademesh-secret

# gRPC Client for Market Data Service (using env var with fallback)
quarkus.grpc.clients.market.host=${MARKET_DATA_SERVICE_HOST:localhost}
quarkus.grpc.clients.market.port=9001

# OpenShift Deployment Configuration
%prod.quarkus.kubernetes.resources.limits.memory=512Mi
%prod.quarkus.kubernetes.resources.requests.memory=256Mi
%prod.quarkus.kubernetes.resources.limits.cpu=500m
%prod.quarkus.kubernetes.resources.requests.cpu=100m
quarkus.openshift.build-strategy=docker

# OpenTelemetry configuration
quarkus.otel.enabled=true
quarkus.otel.exporter.otlp.endpoint=http://jaeger-collector:4317

# HashiCorp Vault Configuration
quarkus.vault.url=${VAULT_URL:http://localhost:8200}
quarkus.vault.authentication.client-token=${VAULT_TOKEN:root}
quarkus.vault.kv-secret-engine-mount-path=secret

# MicroProfile Health
quarkus.smallrye-health.root-path=/health
